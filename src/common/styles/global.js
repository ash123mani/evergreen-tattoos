import { createGlobalStyle } from 'styled-components'

const GlobalStyleVars = createGlobalStyle`
  :root {
    --dark-black: #000;
    --medium-black: #08151d;

    --dark-white: #fff;
  }
`

export default GlobalStyleVars
