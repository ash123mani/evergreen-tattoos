import styled from 'styled-components'

export const CategoryCards = styled.section`
  width: 100%;
  padding: 24px 0px;
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(400px, 1fr));
  gap: 40px;
`
